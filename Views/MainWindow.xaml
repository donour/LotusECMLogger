<Window x:Class="LotusECMLogger.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LotusECMLogger.Views"
        xmlns:vm="clr-namespace:LotusECMLogger.ViewModels"
        mc:Ignorable="d"
        Title="LotusECMLogger" Height="600" Width="800"
        MinWidth="600" MinHeight="400">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0">
            <MenuItem Header="File">
                <MenuItem Header="Exit" Click="OnExitClick"/>
            </MenuItem>
            <MenuItem Header="Help">
                <MenuItem Header="About" Click="OnAboutClick"/>
            </MenuItem>
        </Menu>

        <!-- Control Panel -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,10,0">
                <Button Content="Start" 
                        Command="{Binding StartLoggingCommand}"
                        Width="80" Margin="0,0,5,0"/>
                <Button Content="Stop" 
                        Command="{Binding StopLoggingCommand}"
                        Width="80"/>
            </StackPanel>

            <ComboBox Grid.Column="1" 
                      ItemsSource="{Binding AvailableConfigurations}"
                      SelectedItem="{Binding SelectedConfiguration}"
                      Margin="0,0,10,0"/>

            <Button Grid.Column="2" 
                    Content="Refresh" 
                    Command="{Binding RefreshConfigurationsCommand}"
                    Width="80"/>
        </Grid>

        <!-- Main Content -->
        <TabControl Grid.Row="2" Margin="10,0,10,10">
            <!-- Live Data Tab -->
            <TabItem Header="Live Data">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Search Bar -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox Text="{Binding LiveDataViewModel.SearchFilter, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,10,0"/>
                        <Button Grid.Column="1" 
                                Content="Clear" 
                                Command="{Binding LiveDataViewModel.ClearFilterCommand}"
                                Width="80"/>
                    </Grid>

                    <!-- Data Grid -->
                    <ListView Grid.Row="1" 
                              ItemsSource="{Binding LiveDataViewModel.FilteredReadings}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Parameter" 
                                              DisplayMemberBinding="{Binding ParameterName}"
                                              Width="200"/>
                                <GridViewColumn Header="Value" 
                                              DisplayMemberBinding="{Binding FormattedValue}"
                                              Width="150"/>
                                <GridViewColumn Header="Time" 
                                              DisplayMemberBinding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}"
                                              Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>

            <!-- ECU Coding Tab -->
            <TabItem Header="ECU Coding">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Controls -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding ECUCodingViewModel.SearchFilter, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,10,0"/>
                        
                        <ComboBox Grid.Column="1" 
                                  ItemsSource="{Binding ECUCodingViewModel.Categories}"
                                  SelectedItem="{Binding ECUCodingViewModel.SelectedCategory}"
                                  Width="120" 
                                  Margin="0,0,10,0"/>

                        <ToggleButton Grid.Column="2"
                                    Content="Group by Category"
                                    IsChecked="{Binding ECUCodingViewModel.GroupByCategory}"
                                    Width="120"/>
                    </Grid>

                    <!-- Coding Data -->
                    <ListView Grid.Row="1" 
                              ItemsSource="{Binding ECUCodingViewModel.FilteredOptions}">
                        <ListView.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" 
                                                   FontWeight="Bold" 
                                                   Margin="0,10,0,5"/>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ListView.GroupStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Option" 
                                              DisplayMemberBinding="{Binding Name}"
                                              Width="200"/>
                                <GridViewColumn Header="Value" 
                                              DisplayMemberBinding="{Binding Value}"
                                              Width="150"/>
                                <GridViewColumn Header="Category" 
                                              DisplayMemberBinding="{Binding Category}"
                                              Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock>
                    <Run Text="Refresh Rate:"/>
                    <Run Text="{Binding RefreshRate, StringFormat=F1}"/>
                    <Run Text="Hz"/>
                </TextBlock>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding CurrentLogFile}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window> 